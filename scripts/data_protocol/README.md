# Data Protocol Scripts

Create, materialise, summarise, and validate dataset manifests and fixed
evaluation subsets. The locked CSV membership under
`../../outputs/01_protocol/` must not be randomly resampled during
reproduction.

Use `verify_locked_protocols.py` for the primary protocol-integrity check.
