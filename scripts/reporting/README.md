# Comparison Reporting

`build_adaptive_full_comparison.py` rebuilds the aligned cross-stage comparison
tables under `../../outputs/07_adaptive_full_comparison/` from retained stage
evidence.

Run it from the repository root so relative evidence paths resolve correctly.

