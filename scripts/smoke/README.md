# Public Smoke Protocol

`run_public_smoke_protocol.py` exercises the downstream pipeline with the
synthetic fixture in `../../tests/fixtures/public_micro/`. It requires neither
CASTLE frames nor private model assets.

The smoke run checks integration and reproducibility. It is not a substitute
for the locked research evaluations.

