# Annotation Review Scripts

Build reviewed face-condition categories, generate face proposals, launch the
face reviewer, and validate saved annotations.

Run from the repository root. Use `validate_face_annotations.py` to check the
retained face annotations without changing protocol membership.
