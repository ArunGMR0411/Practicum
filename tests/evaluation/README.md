# Evaluation Tests

Tests perceptual, OCR, FID, and face re-identification evaluation components,
including batching and metric-contract behaviour.

Run with:

```bash
.venv/bin/python -m pytest tests/evaluation
```

