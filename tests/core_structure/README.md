# Core Structure Tests

Checks repository structure and compute-policy behaviour, including expected
configuration and package boundaries.

Run with:

```bash
.venv/bin/python -m pytest tests/core_structure
```

