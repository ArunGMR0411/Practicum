# Test Fixtures

Small, non-private inputs used by lightweight tests.

`public_micro/` contains generated synthetic images, reviewed-style
annotations, and its fixture generator. It does not contain CASTLE frames.

