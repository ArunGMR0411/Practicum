# Public micro-fixture (licence-safe)

Synthetic face/text/screen-like images generated with pure Pillow drawing
(no photographs, no CASTLE, no private weights). Used by clean-clone tests to
exercise App workflow contracts without local assets.

Regenerate:

```bash
python tests/fixtures/public_micro/generate_fixture.py
```
