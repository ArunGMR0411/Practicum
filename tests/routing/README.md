# Routing Tests

Tests router coverage, decision-framework selection, grouped held-out
boundaries, and cross-view subset construction.

Run with:

```bash
.venv/bin/python -m pytest tests/routing
```

