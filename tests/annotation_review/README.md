# Annotation Review Tests

Checks the reviewed face-annotation workflow, saved-box handling, and protocol
validation behaviour.

Run with:

```bash
.venv/bin/python -m pytest tests/annotation_review
```

