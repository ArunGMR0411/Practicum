[pytest]
testpaths = tests
markers =
    e2e_assets: requires local CASTLE frames, model weights, App inputs, or generated image dumps
    public: clean-clone safe (synthetic fixtures / no private assets)
filterwarnings =
    ignore::DeprecationWarning
addopts = -q
