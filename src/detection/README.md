# Detection Components

Interfaces and implementations for face, text, and screen detection. The
package includes individual detector adapters, multimodal stacks, detector
fusion, fallback policies, and the retained error-hardened face policy.

Reviewed ground-truth boxes are evaluation inputs and are not substituted for
detector-produced boxes during normal execution.

