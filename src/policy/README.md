# Policy Components

Loads and validates the machine-readable method and fallback registry in
`../../configs/policy_registry.json`. The registry provides a shared source of
method identifiers and policy metadata for research and App execution.

