# Shared Utilities

Runtime configuration, compute and device profiles, policy checks, logging,
timing, and system detection used across the repository.

Keep hardware-derived recommendations separate from scientific method scores;
tests for both behaviours are under `../../tests/`.
