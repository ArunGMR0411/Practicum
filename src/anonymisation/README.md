# Anonymisation Components

Common anonymiser interface, deterministic face protection, text and screen
redaction, method registry, and adapters for evaluated advanced methods.

Adapters report backend availability and failures explicitly. They do not
bundle third-party weights or override upstream licence requirements.

