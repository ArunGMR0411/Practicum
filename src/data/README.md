# Data Components

CASTLE loading, path configuration, fixed-subset definitions, and subset
construction helpers. Private raw images remain under the local `data/`
boundary; reviewed protocols and annotations are retained under `outputs/`.

