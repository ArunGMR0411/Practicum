# Protocol artefacts (locked evaluation membership)

Protocols are **frozen membership lists**. Regeneration must **not** re-sample CASTLE.

## Face 500 (detection / anonymisation / advanced)

| Manifest | Path |
| --- | --- |
| Face detection 500 | `thesis_manifests/final_face_detection_500.csv` |
| Face anonymisation 500 | `thesis_manifests/final_face_anonymisation_500.csv` |
| Advanced methods 500 | `thesis_manifests/final_advanced_methods_500.csv` |

### Construction / provenance

1. **Subset definitions:**

   `src/data/subset_definitions.py` provides `DETECTION_EVAL_SUBSET` and
   `ANONYMISATION_EVAL_SUBSET`.

2. **Materialisation scripts:**

   `scripts/data_protocol/build_eval_subsets.py`, with supporting development,
   calibration, and FID builders in the same directory. Canonical locked
   manifests are retained under `outputs/01_protocol/thesis_manifests/`.
3. **Verification (do not re-sample):**  
   `scripts/data_protocol/verify_locked_protocols.py`  
   Writes `outputs/01_protocol/02_locked_protocol_verification.json`

Face annotation enrichment (boxes/labels) is documented under  
`annotations/face_detection/03_summary/README.md`.

## Multimodal 250

| Artefact | Path |
| --- | --- |
| Protocol membership | `thesis_manifests/final_multimodal_250.csv` |
| Reviewed boxes | `annotations/multimodal_250/reviewed_multimodal_250_with_boxes.csv` |

Construction: reviewer-curated locked set (not generic random re-sample). Verify with the same `verify_locked_protocols.py` script.

## Seeds

- Face 500 membership is fixed in the CSVs (`selection_seed` / protocol fields where present).  
- Multimodal 250 is reviewer-curated and must not be regenerated by random sampling.
