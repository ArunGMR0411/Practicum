# Face Detection Annotation Summary

This folder summarizes the two 500-image face-detection annotation protocols. Each protocol folder contains one enriched manifest and its subgroup CSV files. The enriched manifest contains the per-image manual-review status, category labels, face count, and reviewed face boxes encoded as JSON.
