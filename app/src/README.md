# App Source

`privacy_pipeline_app/` contains the web interface, wizard state, detection
reviewer, method catalogue, compute-aware recommendations, execution runner,
and reporting workflow used by the demonstrator.

The research pipeline shared with the experiments remains under `../../src/`.

