# App Examples

Synthetic examples for App testing. No CASTLE images are included.

- `anonymised/`: protected outputs.
- `side_by_side/`: comparison previews.
- `metadata/demo_run_manifest.csv`: selected and applied methods.
- `manifest_example.csv`: input manifest example.

The NullFace example records a layered fallback when the backend is unavailable.
