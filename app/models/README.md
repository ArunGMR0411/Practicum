# App models (local only)

Place model weights here. They are **gitignored** and must not be committed.

Required for the core App:

| File | Purpose |
| --- | --- |
| `multimodal_screen_yolo11s.pt` | Screen detection (evaluated project weight) |

Full placement steps (core + research backends): **`app/README.md`**.
