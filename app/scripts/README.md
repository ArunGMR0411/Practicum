# App Launch Scripts

Convenience launchers for the web demonstrator:

- `launch_web.sh` for Linux and macOS.
- `launch_web.bat` for Windows.

Run them from the repository root after installing the dependencies described
in `../README.md`.

